/**
 * Show search results connected
 */
Class.create("Search", {

  initialize : function(item){
    this.item = item
  },

  update : function(data, diff){
    var self = this
    var update = data || diff
  }
})
