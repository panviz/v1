Class.create("GridView", Pane, {
	//__implements : ["Focusable", "ContextMenuable"],

	/**
	 * Constructor
	 */
	initialize : function ($super, oElement, options)
	{ 
			$super(oElement, options);
	}
})
