ViewManager:
Modal:
Bootstrap:
  "dom:loaded":
    "loadBootConfig":
      init:
        Application:
          ActionsManager:
            observe: "app:context_changed", "app:selection_changed", "app:user_logged"
        app.init:
          observe: "app:context_changed"
          "loadRegistry":
            fire: "app:registry_loaded"
            User:
            fire: "app:user_logged"
            actionBar.initActions
              Action:
              fire: "app:actions_loaded"
              fireContextChange
              fireSelectionChange
            loadActiveRepository
              loadRepository
                repository.loadResources
                _contextHolder.setRootItem
                  fire: "app:root_item_changed"
                  setContextItem
                    fire: "app:context_changed"
                rootItem.load
                  TODO
              fire: "app:repository_list_refreshed"
          buildGUI
          window.observe: "resize"
          initWidgets
            TODO
          fire: "app:gui_loaded"
          fire: "app:actions_loaded"
          setTimeout.fire: "app:loaded"
          .
