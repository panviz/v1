module									should contain server and client side logic alongside

Bootstrap								loads settings.json
	
window
	.bootstrap
	.app									client application
	.THEME								current theme, default is "MyBase"
	.I18n

Item		abstract item to be worked with
				can be file, contact, calendar date, note, etc
	.getPath()						//

Collection								collection of Items, represents selection object, data model as well

Application								loads registry, builds app gui
	.initExtension					load extension configuration from manifest.xml
	.initWidgets 						initialization GUI method
		.window[id] = compRegistry[i-1];	//window will contain access to all GUI components classes
	.guiCompRegistry 				array of GUI components
	.guiActions 						Hash
	.contextMenu						Proto.Menu
	.actionBar							ActionsManager
	.getUserSelection				Collection
	.getContextNode					Abstract Item (current path)
	.getContextHolder				Collection - contains current selection, path, repo

View											widget to display Items
	List
		Table or List or Thumbnails
			.fill								fill table with rows
	Graph

ActionsManager
	.actions								Action
Action
	Ls

InfoPanel									Details for Item

TextEditor.klass		//


